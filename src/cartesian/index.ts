export { CartesianPosition } from './cartesian';
import { LinearInterpolate } from './linear-interpolate';
import { ReflectX, ReflectY } from './reflect-position';

export const CartesianConverters = {
  LinearInterpolate
};

export const CartesianTransformers = {
  ReflectX,
  ReflectY
};
